# requirements.txt — direct dependencies, pinned to exact versions
# ─────────────────────────────────────────────────────────────────
# After first successful install, run:
#   pip freeze > requirements-lock.txt
# and commit both files. CI should use requirements-lock.txt for
# fully reproducible builds.
# ─────────────────────────────────────────────────────────────────

# RSS parsing
feedparser==6.0.11

# HTTP with retry support
requests==2.32.3
urllib3==2.2.3

# SSL certificate bundle
certifi==2024.12.14

# Character-set detection (used by requests + trafilatura)
charset-normalizer==3.4.1

# Full-text extraction — no NLTK corpus download required
trafilatura==1.12.2

# trafilatura's primary HTML parser
lxml==5.3.1

# Date extraction from article HTML
htmldate==1.9.3
